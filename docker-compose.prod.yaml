version: "3.9"

services:
  app:
    image: registry.gitlab.com/ivea-water/ivea/web:main
    container_name: ivea-django
    ports:
      - "8008:8000"
    env_file:
      - .env.production
    entrypoint: /code/entrypoint.sh
    tty: true
    stdin_open: true
